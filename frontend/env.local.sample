## AI Provider API Keys (when using separte ai connnection other than vercel connection)
NEXT_PUBLIC_API_URL=http://localhost:8080/
# OPENAI_API_KEY=키는 제이크 문의 후 여기에 적고, 주석처리 지우기
# 파일명에서 .sample 부분 삭제 그리고 파일명 앞에  . 추가 후 저장
# 그러므로 env.local.sample -> env.local -> .env.local